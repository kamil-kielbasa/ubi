# Kconfig Reference

```{note}
**Stub page — content lands in PR 3 of the v1.0.0 documentation restructure.**

Until then, the canonical material lives in {doc}`configuration` § *Kconfig
Options*.
```

This page will host the full enumerated Kconfig reference, separated from
the configuration *guide* (which stays in {doc}`configuration`).
