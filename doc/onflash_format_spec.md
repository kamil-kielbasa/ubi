# Secure On-Flash Format Specification

```{note}
**Stub page — content lands in PR 4 of the v1.0.0 documentation restructure.**

Until then, the normative material is split across:

- {doc}`secure_architecture` — full design and on-flash layout
- {doc}`secure_volume_lifecycle` — provisioning and key rotation events
- {doc}`secure_recovery_notes` — recovery scenarios
- {doc}`secure_runtime_policy` — runtime constraints
```

This page will merge the four documents above into a single normative
specification aimed at implementers and auditors. Developer-facing material
moves to {doc}`secure_overview` and {doc}`secure_workflow`.
