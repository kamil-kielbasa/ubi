# Secure UBI Workflow

```{note}
**Stub page — content lands in PR 4 of the v1.0.0 documentation restructure.**

Until then, the canonical material lives in:

- {doc}`secure_architecture` — full Secure UBI design
- {doc}`secure_volume_lifecycle` — provisioning and key rotation events
- {doc}`secure_runtime_policy` — runtime constraints and behaviours
```

This page will become the developer-oriented Secure UBI guide: when to
enable, prerequisites (PSA key ID, RNG, allowlist, freshness store), the
freshness callback contract, key rotation (lazy vs forced), event handling,
and retirement.
