# Secure Architecture: Overview

```{note}
**Stub page — content lands in PR 4 of the v1.0.0 documentation restructure.**

Until then, the canonical material lives in {doc}`secure_architecture`.
```

This page will become the developer-targeted Secure UBI overview (≤ 5 pages):

- What Secure UBI does (encrypt + authenticate every on-flash record)
- Key hierarchy — one diagram
- Threat model — table
- Application contract: allowlist, RNG, freshness callback, event callback
- Key lifecycle — four states, one diagram

The full normative spec moves to {doc}`onflash_format_spec`.
